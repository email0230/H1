<UserControl x:Class="h1.Views.ManualAssignmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:h1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <!-- Header with Fixed Position -->
        <Grid>
            <Label Content="Manual Room Assignment" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0" Foreground="Black" />
        </Grid>

        <!-- Room Assignment Form -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,60,0,0">
            <!-- Add your room assignment form controls here -->
            <Label Content="Guest Name:" />
            <TextBox Name="GuestNameTextBox" Width="200" Margin="0,0,0,10" />

            <Label Content="Select a Room:" />
            <ComboBox Name="RoomComboBox" Width="200" Margin="0,0,0,10">
                <!-- Room options will be added dynamically -->
            </ComboBox>

            <Button Content="Assign Room" Click="AssignRoomButton_Click" Width="120" />
        </StackPanel>
    </Grid>
</UserControl>
